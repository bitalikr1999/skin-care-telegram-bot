services:
  bot:
    build: .
    container_name: skin-diary-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SQLITE_PATH: /app/data/skin-diary.sqlite
    volumes:
      - skin_diary_data:/app/data

volumes:
  skin_diary_data:
